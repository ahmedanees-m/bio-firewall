"""v1.1 expanded head-to-head — the four dimensions where a deterministic, artifact-reading CONTROL beats an LLM
ADVISOR in ways no prompt can fix (BIOFIREWALL_BENCHMARK_PROTOCOL_v1.1):

  A fabrication   — asked for a checkable number, the LLM fabricates; the firewall computes or abstains (architectural)
  B determinism   — firewall byte-identical + paraphrase-invariant; the LLM is neither (sampling + surface-form)
  C cargo-on-B1   — the LLM can't score a low-identity toxin homolog from raw sequence; ESM can (no embedding/search)
  D jailbreak     — the LLM-as-judge can be talked out of a refuse via the plan's text; the firewall reads coordinates
  E usability     — is the LLM's over-refusal of legitimate research prompt-fixable? (test honestly)

Each gives the LLM its BEST configuration and shows fundamental, non-prompt-fixable failure. B1 = claude-opus-4-8."""
